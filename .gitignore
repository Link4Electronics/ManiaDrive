build/
!external/php-5.3.27/build/
*.bin
*.o
*.so
*.so.*
*.a
configure.c
configure.bin
configure.log
configure.conf
test
compile_commands.json
# External dependencies — re-fetch with:
#   cmake -DODE_SOURCE_DIR=   (or use the external/ODE checkout)
#   cmake -DPHP_EXTRACTED_DIR= (or use the external/php-5.3.27 extraction)

